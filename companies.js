function clearCompanyForm(){companyId.value="";companyName.value="";companyWhatsapp.value="";companyActive.value="yes";}
function saveCompany(){const id=companyId.value||uid("co");const company={id,name:companyName.value.trim(),whatsapp:companyWhatsapp.value.trim(),active:companyActive.value};if(!company.name)return alert("Company name required");const index=db.companies.findIndex(c=>c.id===id);if(index>=0)db.companies[index]=company;else db.companies.push(company);clearCompanyForm();saveDB();}
function editCompany(id){const company=db.companies.find(c=>c.id===id);if(!company)return;companyId.value=company.id;companyName.value=company.name;companyWhatsapp.value=company.whatsapp;companyActive.value=company.active;}
function deleteCompany(id){if(!confirm("Delete company?"))return;db.companies=db.companies.filter(c=>c.id!==id);db.products=db.products.filter(p=>p.companyId!==id);db.customers.forEach(c=>{c.companyAccess=(c.companyAccess||[]).filter(x=>x!==id);});saveDB();}
function renderCompanies(){companyList.innerHTML="<table><tr><th>Company</th><th>WhatsApp</th><th>Status</th><th>Action</th></tr>"+db.companies.map(c=>`<tr><td>${esc(c.name)}</td><td>${esc(c.whatsapp)}</td><td>${esc(c.active)}</td><td><button class="btn secondary" onclick="editCompany('${c.id}')">✏️ Edit</button> <button class="btn danger" onclick="deleteCompany('${c.id}')">Delete</button></td></tr>`).join("")+"</table>";}