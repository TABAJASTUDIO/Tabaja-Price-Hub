function renderOfferPicker(){const companyId=offerCompany.value;const q=(offerSearch.value||"").toLowerCase();const rows=db.products.filter(product=>product.companyId===companyId&&product.name.toLowerCase().includes(q));offerPicker.innerHTML="<table><tr><th>Product</th><th>CTN</th><th>BAG</th><th>Offer Price</th><th>Action</th></tr>"+rows.map(p=>`<tr><td>${esc(p.name)}</td><td>${p.ctn!==""&&p.ctn!==undefined?money(p.ctn):"-"}</td><td>${p.bag!==""&&p.bag!==undefined?money(p.bag):"-"}</td><td><input id="of_${p.id}" value="${p.offerPrice||""}" placeholder="Offer price"></td><td><button class="btn secondary" onclick="setOffer('${p.id}')">Save Offer</button></td></tr>`).join("")+"</table>";}
function setOffer(id){const product=db.products.find(p=>p.id===id);const value=document.getElementById("of_"+id).value;product.offerPrice=value;product.isOffer=value?"yes":"no";saveDB();}